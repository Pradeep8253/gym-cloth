import React from 'react';
import RegisterForm from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Register | VOLT ATHLETICS',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
